VulkanBaseクラスの設計の概要

基本的には、インスタンス化して利用する。

<必要な機能>
Objファイルのパス、ワールド空間上の座標、ワールド空間上の回転、スケールを渡すと、モデルが表示される。

クラス外から何らかの方法でオブジェクトを指定してオブジェクトをコピーできる。

オブジェクトを指定して、マテリアルをセットできる。
2024/4/26現在


GameManagerクラスは、モデルやテクスチャのロードを行う。